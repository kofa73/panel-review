Your previous response in a blind code review could not be parsed by the strict
JSON validator. This is a FORMATTING fix ONLY — keep your findings/stances exactly
as you meant them: do NOT re-review, do NOT add or remove items, do NOT change any
conclusion or severity. Only correct the JSON shape so each item validates.

## Your previous response (verbatim)
{{PREV}}

## What the validator rejected (one entry per bad item: the reason, then the offending text)
{{VIOLATIONS}}

## Target schema
Each line inside the block below is one item in exactly this shape:
{{SCHEMA}}

## Re-emit
Output ONLY one fenced code block — nothing before or after it — whose info string
(the word immediately after the opening fence) is exactly this tag:
{{TAG}}
Inside the block put JSONL: one compact JSON object per line, each matching the schema above.

Carry over the substance you already produced, do not re-review, and do not invent anything new:
- If your previous response already listed items (in a block or in prose), extract each one you
  already stated into the block — re-home any misplaced fields into the shape the schema and the
  messages above describe (for findings, the evidence facts and any `precondition`/`impact` belong
  INSIDE a `points[]` entry alongside its `assertion` and `location`, never at the top level).
- Emit an EMPTY block (no lines) **if and only if** your previous response genuinely contained no
  items. Do NOT fabricate an item to fill the block, and do NOT drop items you actually stated.
- Items that were already valid must be reproduced as-is. If one item genuinely cannot be fixed
  without information you no longer have, drop just that one item rather than guessing.
